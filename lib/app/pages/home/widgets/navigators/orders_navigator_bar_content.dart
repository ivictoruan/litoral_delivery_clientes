import 'package:flutter/material.dart';

class OrdersNavigatorBarContent extends StatelessWidget {
  const OrdersNavigatorBarContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Meus pedidos'),
      ),
      body: Container(),
    );
  }
}
