
import 'package:flutter/material.dart';

class ProfileNavigatorionBarContent extends StatelessWidget {
  const ProfileNavigatorionBarContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Profile Screen'),
    );
  }
}